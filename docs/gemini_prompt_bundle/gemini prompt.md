# Gemini Prompt：生成 14 页课程答辩 PPT（可直接粘贴）

你是一名专业的中文技术答辩 PPT 设计助手。请严格根据以下内容生成 **14 页** PPT，输出为可编辑演示稿（中文）。

如果我同时提供样例图片 `有效的一张ppt样例.png`，请将其**仅作为风格参考**：
- 学习其信息层级、留白节奏、标题重量、结论横条、中心主图容器、底部要点收口这些抽象特征
- 不要直接复制该图的标题文案、模块内容、具体框图结构、节点位置、连线方式、比例关系或整页构图
- 最终页面应与样例图“气质接近、结构合理”，但必须是针对本页内容重新设计后的新页面


## 1）全局要求（硬性，必须逐条满足）

- 语言：中文
- 场景：课程答辩
- 风格：**学术答辩展示风**，克制、专业、清爽，不要花哨，但也不要过度冷淡或空
- 版式：每页固定“标题 + 关键结论 + 证据要点”
- 事实约束：**不得编造**，只能使用我提供的文字与数据
- 内容密度：每页 3–6 条核心要点，数字保留到 4 位小数即可
- 图示页：第 3 页与第 10 页必须直接插入对应架构图素材，不使用占位图，不保留“后续替换”提示

**禁止项（任何一条出现都算不合格）**：
- 禁止渐变背景、插画风背景、纹理底图、照片蒙版
- 禁止明显阴影、发光、玻璃拟态、3D 效果、拟物图标
- 禁止复杂装饰图形（波浪、气泡、彩色大色块拼贴）
- 禁止动画感文案（如“酷炫”“震撼”）与营销化措辞
- 禁止同页超过 2 种主色、超过 3 档字号层级

**统一规则**：
- 页面比例：16:9
- 全文左对齐优先，避免居中堆叠
- 每页只保留 1 个重点视觉元素（表格或架构图二选一）
- 保持足够留白，但不要为了极简而显得空；页面需要有完整的结构承托
- 页序必须与本提示一致（第 1 页到第 14 页），不得合并、拆分或重排页面
- 不要假设可读取、套用、继承或适配任何 `.pptx` 模板；直接按本提示完成完整 14 页排版
- 每页仍遵循“标题 + 关键结论 + 证据要点”，不得改变信息结构

**图示素材映射（同目录文件）**：
- 第 3 页“训练/推理架构图”优先使用：`page3_训练推理架构图.png`
- 第 10 页“线上大模型辅助增强方案”优先参考：`线上大模型辅助增强方案.png`

---

## 2）逐页完整文案（1–14）

## 第 1 页｜封面
**标题**：多媒体技术课设答辩  
**副标题**：AI 伪造图像检测与解释：检测（分类）+ 解释（文本）+ 定位（分割）  
**答辩人**：谢鹏、陈冠宇  

**页内要点**：
- 项目目标：同一模型同时完成真假判断、伪造原因解释、伪造区域定位
- 汇报主线：数据选择 → 训练架构 → 两轮 smoke/正式测试 → 线上增强方案 A/B/C

---

## 第 2 页｜数据集选择
**标题**：数据集选择：互补融合  
**关键结论**：课程数据与 GenImage 在任务维度上互补，混合使用才能支撑三任务统一训练。

**页内要点**：
- 原计划直接使用课程参考数据集，但分类训练信息不足
- 进一步寻找包含分类信息的数据集，选定 GenImage
- GenImage 有分类信息，但缺少解释与定位标注
- 课程数据更适合解释/定位，GenImage 提供分类信号
- 最终方案：混合数据训练统一多任务模型

---

## 第 3 页｜训练架构
**标题**：训练架构：混合数据下的统一多任务框架  
**关键结论**：通过共享骨干 + 三任务分支，并结合 partial labels，实现多源异构数据的联合训练。

**页内要点**：
- 单张图输入共享 backbone
- 同时输出：分类 logits、解释特征、定位 mask logits
- 使用 has_cls / has_exp / has_loc 控制不同样本参与对应任务损失
- 训练与推理链路保持一致，便于线上部署与评估

**图示要求**：
- 在页面中部直接插入“训练/推理架构图”素材

---

## 第 4 页｜训练策略
**标题**：训练策略：A/B 两阶段“先热身再联合”  
**关键结论**：smoke 测试用于正式训练前的低成本链路验证；验证通过后，再进入 Stage A 分类热身与 Stage B 联合训练。

**页内要点**：
- smoke：在正式训练开始前先跑通三任务链路，确认分类、解释、定位都能正常训练/评估
- Stage A：仅做分类热身（先稳定真假判别）
- Stage B：继续保留分类训练，同时叠加解释与定位监督，进行联合训练
- 时序是“smoke → Stage A → Stage B”，不要理解为 smoke 插在 A/B 之间
- 单图推理用于检查可视化输出是否合理

---

## 第 5 页｜第一次冒烟测试
**标题**：第一次冒烟测试：链路打通，但定位为 0  
**关键结论**：三任务流程已跑通，但定位监督链路存在问题，需优先修复。

**页内要点（首次 smoke）**：
- Acc = 1.0000
- AP = 1.0000
- BLEU = 0.4069
- ROUGE-L = 0.2893
- IoU = 0.0000
- PixF1 = 0.0000

**页内要点（用 smoke 模型验收课程数据）**：
- Acc = 0.5950
- AP = 0.6990
- BLEU = 0.4383
- ROUGE-L = 0.2923
- IoU = 0.0000
- PixF1 = 0.0000

---

## 第 6 页｜问题修复
**标题**：问题修复：定位全 0 的根因与闭环验证  
**关键结论**：修复 mask 前景值后，定位指标恢复非零，单图推理 mask 不再纯黑。

**问题原因**：
- `polygon_to_mask()` 前景值错误设为 1
- 经 `ToTensor()` 后前景变为 1/255
- 在 `>0.5` 阈值下被全部清空
- 导致训练与评估长期使用全零 mask 监督

**修复方式与结果**：
- 前景值由 1 改为 255
- 修复后 smoke：
  - IoU = 0.1048
  - PixP = 0.3013
  - PixR = 0.1662
  - PixF1 = 0.2142
- 再次单图推理：预测 mask 不再是纯黑图

---

## 第 7 页｜分类偏低诊断
**标题**：诊断分类偏低：关键是生成器域偏移  
**关键结论**：主问题不是阈值，而是 fake 生成器覆盖不足。

**页内要点**：
- 第一次正式测试后发现分类偏低（该次结果未备份）
- 课程 fake 来源不只一种，包含：
  - adm、biggan、glide/vqdm(1000)、midjourney、sdv4、sdv5、wukong
- 训练 fake 数据当时仅来自 `adm`
  - train/ai = 162000（adm）
  - val/ai = 6000（adm）
- 结论：存在明显生成器域偏移
- 改动：新增 `biggan` 数据源，配置由 `imagenet_root` 扩展为 `imagenet_roots`

---

## 第 8 页｜第二次冒烟测试
**标题**：第二次冒烟测试：多数据源改动接线成功  
**关键结论**：多数据源流程正常打通；smoke 指标用于验证链路，不代表最终上限。

**页内指标**：
- Acc = 0.5224
- AP = 0.4470
- BLEU = 0.4260
- ROUGE-L = 0.3270
- IoU = 0.0752
- PixP = 0.1519
- PixR = 0.1884
- PixF1 = 0.1682

---

## 第 9 页｜第二次正式测试
**标题**：第二次正式测试：分类与定位显著提升  
**关键结论**：修复监督 + 多数据源训练带来正向提升，但未覆盖生成器仍是短板。

**课程验收总指标**：
- Acc = 0.7000
- AP = 0.8363
- BLEU = 0.4271
- ROUGE-L = 0.2985
- IoU = 0.1501
- PixP = 0.3447
- PixR = 0.2673
- PixF1 = 0.3011

**按生成器拆分（Acc / fake recall / avg fake score）**：
- adm：0.88 / 0.88 / 0.8766
- biggan：1.00 / 1.00 / 1.0000
- glide/vqdm1000：0.12 / 0.12 / 0.1595
- midjourney：0.16 / 0.16 / 0.1886
- sdv4：0.64 / 0.64 / 0.5880
- sdv5：0.40 / 0.40 / 0.4350
- wukong：0.24 / 0.24 / 0.2800
- real：Acc 0.955 / avg fake score 0.0731

**结论补充**：
- 模型已学会 adm、biggan
- 对 glide/vqdm1000、midjourney、wukong 等未覆盖生成器泛化明显不足

---

## 第 10 页｜线上模型辅助增强
**标题**：线上模型辅助增强：方案设计  
**关键结论**：围绕线上大模型辅助增强，重点设计并分析 3 个方案，目标是在不破坏本地主链路前提下提升效果。

**页内要点**：
- 方案 A：解释增强（远程只增强 explanation）
- 方案 B：难例复判（本地先判，边界样本路由远程）
- 方案 C：离线蒸馏（teacher 输出蒸馏回本地模型）
- 采用模型：qwen3-vl-plus

**图示要求**：
- 参考 `线上大模型辅助增强方案.png` 重新组织本页图示
- 只保留并展示方案 A / B / C，不要在页面中提及 Plan D
- 不要直接照搬原图，需按本页内容重新排版为新的答辩页

---

## 第 11 页｜方案 A（解释增强）
**标题**：方案 A：解释增强（200 样本）  
**关键结论**：自动指标没有超过本地基线，整体效果不理想。

**设置**：
- 本地模型继续负责分类与定位
- 远程多模态模型输入：原图 + 预测 mask + fake_score + top-k caption
- 远程模型输出更自然、更细致解释文本

**全量结果（200 张）**：
- local BLEU = 0.4647
- remote BLEU = 0.3806
- local ROUGE-L = 0.3016
- remote ROUGE-L = 0.2986
- remote BLEU 更优样本数 = 23 / 200（11.5%）
- remote ROUGE-L 更优样本数 = 105 / 200（52.5%）

**结论**：表面上看并不好，但可能仅仅是因为线上解释风格跟本地训练模型相比与测试集差异更大导致硬性比较的数据更差。

---

## 第 12 页｜方案 B（难例复判）
**标题**：方案 B：本地先判 + 难例路由远程复判  
**关键结论**：本版 Plan B 导致分类下降，且线上稳定性/一致性是主要瓶颈。

**课程验收全量结果（Plan B）**：
- 分类：Acc = 0.6045，AP = 0.7943
- 解释：BLEU = 0.4124，ROUGE-L = 0.2981
- 定位：IoU = 0.1501，PixP = 0.3447，PixR = 0.2673，PixF1 = 0.3011

**policy 统计（classification 子集）**：
- total_cases = 402
- routed_cases = 81（20.15%）
- remote_response_cases = 79（97.53% of routed）
- remote_agreement_rate = 18.99%
- override_cases = 64（79.01% of routed）
- fallback_cases = 2（2.47% of routed）

**policy 统计（explanation 子集）**：
- total_cases = 200
- routed_cases = 25（12.5%）
- remote_response_cases = 22（88.0% of routed）
- remote_agreement_rate = 13.64%
- override_cases = 19（76.0% of routed）
- fallback_cases = 3（12.0% of routed）
 
**结论**：效果不好，可能是因为远程通用模型的鉴别能力比本地训练模型还差，这后续可以通过针对远程模型的测试来检验。

---

## 第 13 页｜方案 C（离线蒸馏）
**标题**：方案 C：离线蒸馏（按主口径）  
**关键结论**：当前蒸馏不是整体提升，而是“分类/解释下降、定位上升”的分化。

**流程**：
- 使用 `distill/run_llm_distill_pipeline.py`
- 基于 `qwen3-vl-plus` 完成 teacher 采集、cache 构建与蒸馏训练
- 得到蒸馏模型：`outputs_llm_distill/best.pt`

**课程验收对比**：
- 蒸馏模型：
  - Acc = 0.6766
  - AP = 0.8036
  - BLEU = 0.3996
  - ROUGE-L = 0.2765
  - IoU = 0.1711
  - PixP = 0.3640
  - PixR = 0.3059
  - PixF1 = 0.3324
- 基线模型：
  - Acc = 0.6990
  - AP = 0.8325
  - BLEU = 0.4618
  - ROUGE-L = 0.3079
  - IoU = 0.1501
  - PixP = 0.3447
  - PixR = 0.2673
  - PixF1 = 0.3011

**差异总结**：
- 定位提升：IoU +0.0210，PixF1 +0.0313
- 分类与解释下降：Acc、AP、BLEU、ROUGE-L 均下降

**可能原因**：
- teacher 覆盖率偏低（约 1000 vs Stage B 联合训练约 3 万）
- 分类 teacher 被压缩为两类伪 logits，监督信息不足
- explanation teacher 文本分布与课程标注分布不一致

---

## 第 14 页｜致谢
**标题**：致谢  
**关键结论**：汇报完毕，感谢各位老师与同学的聆听和指导。

---

## 3）版式与视觉细节（给 Gemini 的设计指令，强调“克制但完整”）

- 背景：纯白（#FFFFFF）
- 正文字色：深灰近黑（#111111）
- 强调色：仅 1 个（#1F4E79）
- 可辅以极浅灰或极浅蓝作为分区底色，但整体仍应以白底、深字、单一蓝色强调为主
- 字体：中文无衬线（微软雅黑/思源黑体），全稿统一一种字体
- 标题区：每页顶部左对齐大标题，粗体、黑色、醒目，标题本身就是最强视觉锚点
- “关键结论”区：放在标题下方，做成横向结论条；使用细蓝边框或极浅蓝底，文字尽量一行说清，可有非常轻微的层次承托
- 页面允许使用少量容器、细分割线、浅底块来组织信息，但必须服务于信息，不可为了装饰而装饰
- 指标页：使用简洁表格（细边框、浅底色可选、无花哨底纹），必要时可搭配 1 个小型结论卡片
- 第 5/8/9/11/12/13 页放指标表时，优先“1 张表 + 2 条结论”
- 图示页中部应有清晰主视觉区：可使用大面积浅蓝白底圆角容器承载架构图，但必须克制、干净、细描边
- 第 3/10 页直接放入对应架构图素材，不加额外装饰，不保留占位框；可在素材外使用克制的统一容器承载
- 图示页底部再用 2–3 组短 bullet 做总结，形成“上标题 - 中主图 - 下要点”的稳定结构
- 非图示页也尽量延续这种层级关系：上方给结论，中部给证据主体，下方或右侧少量收口
- 允许出现“正式答辩感”的版面完成度：例如结论横条、数据分组框、统一圆角卡片、轻分区背景
- 不要把页面做成只有标题和几行字的极端极简稿；需要像完成度较高的课程答辩 PPT
- 样例图可参考其“学术海报式”的秩序感，但不要复刻其中框图布局、卡片数量、文本内容或形状摆位
- 页脚统一放“AI 伪造图像检测与解释 · 课程答辩”

**强制排版参数**：
- 页边距：左右 ≥ 6%，上下 ≥ 7%
- 行距：1.2–1.35
- 每条要点尽量单行，超长则分两行
- 图示页主图区宽度建议占内容区的 65%–82%，避免图过小
- 页内不同元素之间必须有明确间距，不要把标题、结论条、主图、要点挤在一起
- 建议每页至少形成 2 层信息区块，不要所有内容都裸排在白底上
- 禁止使用艺术字、描边字、渐变字

---

## 4）输出要求

请直接输出可演示的完整 PPT（14 页），并保证：
- 页序与上述一致
- 所有数字与文字不被改写、不被杜撰
- 视觉统一、适合课堂答辩投影
- 追求“克制、清楚、完整”的答辩质感，而不是极端极简；允许轻度分区与层次，但不得花哨
- 每页不超过 6 条要点；每页仅 1 个重点视觉元素
- 输出前请先自检：是否违反“禁止项”，若违反请先修正再输出
